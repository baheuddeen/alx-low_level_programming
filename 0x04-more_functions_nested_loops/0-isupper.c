#include "main.h"

/**
 * _isupper -  checks for uppercase character.
 * @c: characte to check.
 * Return: 1 if Upper and 0 if not.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'A')
		return (1);
	return (0);
}
